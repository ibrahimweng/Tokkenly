{
  "name": "tokkenly",
  "private": true,
  "version": "0.1.0",
  "description": "Tokkenly. The product is in app/, the design record is design.md.",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm ci && npm run build",
    "preview": "cd app && npm run preview",
    "typecheck": "cd app && npm run typecheck"
  },
  "engines": { "node": ">=18" }
}
